import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 440,
  },
  loading: {
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
  },
  textList: {
    fontSize: 30,
    fontWeight: 'bold',
  },
  list: {
    backgroundColor: 'red',
    height: 250,
  },
});
